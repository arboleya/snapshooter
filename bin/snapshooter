#!/usr/bin/env node

// requirements
path = require( 'path' );
fs = require( 'fs' );

// lib folder
lib = path.join( __dirname, '..', 'lib', 'snapshooter.js' );

// require class
Snapshooter = require( lib );

// initializes it
new Snapshooter;